int calculate(int x, int y) {
    int result;
    if (x > 0) {
        result = x * y * 2;
    } else {
        result = x * y * 2;
    }
    return result;
}

int main() {
    int n, a,b,c,i;
    n = 1000;
    for (i = 0; i < n; i = i + 1) { a = i * 2 - i - i; b = 9; c = a * b; }
    printf("%d%d%d", a, b, c);
    return 0;
}